/**
 * Custom Cloudflare Worker entry point.
 *
 * Use this file to add custom Worker-level behavior:
 * - Durable Objects
 * - Queues (producer/consumer)
 * - Cron triggers (scheduled())
 * - Custom fetch handlers wrapping Astro's handler
 *
 * When ready, update wrangler.jsonc:
 *   "workerEntryPoint": { "path": "workers/entry.ts", "namedExports": ["YourExport"] }
 */

export default {
  // fetch: (request, env, ctx) => { ... },
  // scheduled: (controller, env, ctx) => { ... },
};
